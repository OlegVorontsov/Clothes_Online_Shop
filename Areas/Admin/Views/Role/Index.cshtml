@model List<RoleViewModel>

@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    @await Html.PartialAsync("AdminLeft")
    <div class="col-10">
        @if (Model == null || Model.Count == 0)
        {
            <h4 style="padding-left: 0.75rem!important">Прав нет</h4>
        }
        else
        {
            <h4 style="padding-left: 0.75rem!important">Права</h4>
            <table class="table">
                <thead>
                    <tr>
                        <th>Название</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>
                                <a class="btn-danger btn"
                                   asp-area="Admin"
                                   asp-controller="Role"
                                   asp-action="Remove"
                                   asp-route-roleName="@item.Name"
                                   role="button">
                                    Удалить
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        <a class="btn btn-secondary mb-4"
           asp-area="Admin"
           asp-controller="Role"
           asp-action="Add"
           role="button">
            Добавить
        </a>
    </div>
</div>
